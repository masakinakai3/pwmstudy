# 三相PWMインバータ シミュレータ — 利用手順書

## 1. 起動方法

### 1.1 desktop UI

```bash
cd 3lvlpwm
pip install -r requirements.txt   # 初回のみ
python main.py
```

起動するとウィンドウが開き、デフォルトパラメータで波形が表示されます。

### 1.2 web UI / API（直接起動）

```bash
cd 3lvlpwm
pip install -r requirements.txt   # 初回のみ
python -m uvicorn webapi.app:app --reload
```

起動後はブラウザで [http://127.0.0.1:8000/](http://127.0.0.1:8000/) を開きます。

- Web UI: [http://127.0.0.1:8000/](http://127.0.0.1:8000/)
- API health: [http://127.0.0.1:8000/health](http://127.0.0.1:8000/health)
- OpenAPI docs: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

web UI は FastAPI 上で配信される静的フロントエンドです。表示単位は desktop UI に合わせて、V_LL は RMS、f_c は kHz、t_d は us、L は mH で入力します。

### 1.3 web UI / API（Docker）

Docker が使える環境では、Python 環境のセットアップなしに 1 コマンドで起動できます。授業用サーバや共有 PC への配布に適しています。

```bash
cd 3lvlpwm
docker compose up --build
```

起動後はブラウザで [http://localhost:8000/](http://localhost:8000/) を開くだけで利用できます。

| コマンド | 内容 |
| --- | --- |
| `docker compose up --build` | イメージビルドと起動（初回・更新時） |
| `docker compose up -d` | バックグラウンド起動 |
| `docker compose down` | 停止 |
| `docker compose logs -f` | リアルタイムログ確認 |

> **ポート変更が必要な場合**: `docker-compose.yml` の `"8000:8000"` を例えば `"8080:8000"` に書き換えて再起動してください。
> **デスクトップ版との共存**: Docker はコンテナ内に web API のみを含みます。同一マシンで `python main.py` による desktop UI を利用することも引き続き可能です。
> **学内サーバ設置時のセキュリティ**: コンテナは `0.0.0.0`（全インターフェース）でリッスンします。インターネット接続サーバへ直接配置する場合は、ファイアウォールまたはリバースプロキシ（nginx 等）で保護することを推奨します。
> **複数同時利用**: デフォルト設定はシングルワーカー（1 プロセス）です。数十名が同時アクセスする学内サーバ環境では `Dockerfile` の CMD 行にある `--workers 1` を実測負荷に合わせて増やすことを検討してください。

## 2. 画面構成

### 2.1 desktop UI

画面は上から6段の波形パネルと下部の8つのスライダー、PWM 方式選択、学習シナリオボタン、エクスポートボタンで構成されます。

```text
┌────────────────────────────────────────┐
│  m_a = 0.544                           │  ← 変調率表示（左上）
├────────────────────────────────────────┤
│  ① 変調信号 + キャリア                   │  三相正弦波指令(赤/青/緑) + 三角波(灰)
├────────────────────────────────────────┤
│  ② スイッチングパターン                   │  S_u / S_v / S_w（オフセット表示）
├────────────────────────────────────────┤
│  ③ 線間電圧 [V]                         │  v_uv / v_vw / v_wu + 基本波重畳
│                                        │  ベースライン設定時は橙点線でオーバーレイ
├────────────────────────────────────────┤
│  ④ 相電圧 [V]                           │  v_uN + 基本波重畳
├────────────────────────────────────────┤
│  ⑤ 相電流 [A]                           │  i_u / i_v / i_w + 理論電流重畳
│                                        │  ベースライン設定時は紫点線でオーバーレイ
├────────────────────────────────────────┤
│  ⑥ FFTスペクトル                         │  電圧/電流切替 + THD/RMS表示
├────────────────────────────────────────┤
│  Reference: Sinusoidal / 3rd Harmonic / Min-Max Zero-Sequence │
├────────────────────────────────────────┤
│  Sampling: Natural / Regular, Clamp: Continuous / DPWM1 / DPWM2 / DPWM3 │
├────────────────────────────────────────┤
│  FFT: Signal = v_uv / i_u, Window = Hann / Rectangular │
├────────────────────────────────────────┤
│  スライダー: V_dc / V_LL(rms) / f /      │
│             f_c / t_d / V_on / R / L   │
├────────────────────────────────────────┤
│  プリセット: ①線形変調限界 ②キャリア周波数 ③L平滑効果 ④位相遅れ ⑤過変調 ⑥SVPWM比較 │
├────────────────────────────────────────┤
│  JSON保存 / PNG保存 / ベースライン設定 / ベースライン解除 │
└────────────────────────────────────────┘
```

### 2.2 web UI

web UI は次の 3 セクションで構成されます。

```text
┌────────────────────────────────────────┐
│  Hero / API status                     │
├────────────────────────────────────────┤
│  左: 8パラメータ入力 + 変調3軸/FFT切替    │
│  右上: m_a, m_f, THD, RMS, PF1 など      │
├────────────────────────────────────────┤
│  ① 変調信号 + キャリア                   │
├────────────────────────────────────────┤
│  ② 線間電圧 / 相電圧                     │
├────────────────────────────────────────┤
│  ③ 相電流 / FFT                         │
└────────────────────────────────────────┘
```

web UI は desktop UI と完全一致ではありませんが、学習シナリオガイド、ベースライン比較、JSON/PNG 保存、理論比較パネルまでは利用できます。desktop UI の 6 段表示に対し、web UI は主要 3 セクションへ整理されています。

### 各パネルの見方

#### ① 変調信号 + キャリア

- **赤**: U相指令 $v_u^*$、**青**: V相指令 $v_v^*$、**緑**: W相指令 $v_w^*$
- **灰**: 三角波キャリア（振幅±1）
- 指令信号がキャリアより大きい区間でスイッチON
- Regular Sampling では、各キャリア周期の中点でサンプルした値が階段状に保持される
- Third Harmonic Injection では、三相へ共通の三次高調波成分が加わり、線間成分を保ったまま線形変調範囲が拡張される

#### ② スイッチングパターン

- 各相のスイッチング信号（0=OFF, 1=ON）を上下にオフセット表示
- 上段: S_u（赤）、中段: S_v（青）、下段: S_w（緑）

#### ③ 線間電圧

- PWM変調されたパルス状電圧（+V_dc, 0, −V_dc の3レベル）
- 包絡線に基本波の正弦波形が確認できる

#### ④ 相電圧

- 負荷中性点基準の相電圧 $v_{uN}$ を表示
- 4レベル相当のステップ波形の中に基本波が重畳される

#### ⑤ 相電流

- RL負荷を通過することで、PWMリプルが平滑化されたほぼ正弦波の電流
- 黒破線は、相電圧基本波から計算した理論電流波形
- 情報パネルの「FFT実測」は電流の基本波振幅を表し、理論値と同じ物理量で比較している

#### ⑥ FFTスペクトル

- FFT Signal で **線間電圧 $v_{uv}$** と **相電流 $i_u$** を切り替えられる
- FFT Window で **Hann** と **Rectangular** を切り替えられる
- **青**: 基本波成分（出力周波数 $f$ 付近）
- **赤**: キャリア高調波（$f_c$, $2f_c$, $3f_c$ 付近）
- **灰**: その他の高調波
- 右上に **THD, 基本波振幅, 全RMS値** を表示
- 情報パネルには **V1, I1, THD_V, THD_I, PF1(FFT)** が常時表示される

### 変調率表示（左上）

- `m_a = 0.544` のように現在の変調率を表示
- $m_a > 1$ かつ Overmod View 無効時: **クランプ中表示**
- Overmod View 有効時: **過変調観察中表示**（線形上限超過を警告）

## 3. 操作方法

### 3.1 desktop UI

### 変調3軸の選択

#### 参照生成方式

| 方式 | 内容 | 観察ポイント |
| --- | --- | --- |
| Sinusoidal Reference | 基本の三相正弦波参照 | SPWM 系の基準。参照波形は滑らかな正弦波 |
| 3rd Harmonic Injection | 三相共通の三次高調波を注入した参照 | 線間基本波を保ったまま、線形変調範囲を拡張できる |
| Min-Max Zero-Sequence | max/min 零相注入を使う参照 | キャリアベースで SVPWM 相当の利用率を得られる |

#### サンプリング方式

| 方式 | 内容 | 観察ポイント |
| --- | --- | --- |
| Natural Sampling | 連続参照とキャリアを逐次比較 | 基本形。スイッチング時刻は参照とキャリアの交点で決まる |
| Regular Sampling | キャリア周期ごとに中点サンプル値を保持して比較 | 参照波形が階段状になり、自然サンプリングと異なるスイッチングになる |

#### クランプ方式

| 方式 | 内容 | 観察ポイント |
| --- | --- | --- |
| Continuous PWM | 連続PWM。3相とも各区間でスイッチングする | 最も基本的な比較対象 |
| DPWM1 | 山頂・谷底付近で 60 度クランプ | 1相をピーク近傍で休止し、スイッチング回数を減らす |
| DPWM2 | DPWM1 と相補の位置で 60 度クランプ | クランプ位置差で波形が変わる |
| DPWM3 | 山頂の両脇でクランプする M 字型寄りの不連続PWM | DPWM1/2 と異なるクランプ位置を比較できる |

### FFT 表示切替

| 項目 | 選択肢 | 観察ポイント |
| --- | --- | --- |
| FFT Signal | Line Voltage v_uv / Phase Current i_u | 電圧の高調波群と、RL負荷で平滑化された電流スペクトルを比較できる |
| FFT Window | Hann / Rectangular | 非整数周期や近接高調波の見え方を比較できる。Hann は leakage を抑えやすい |

| スライダー | 表示 | 範囲 | ステップ | 効果 |
| --- | --- | --- | --- | --- |
| V_dc | V_dc [V] | 100–600 V | 1 V | 直流母線電圧。出力電圧のパルス高さが変化 |
| V_LL(rms) | V_LL(rms) [V] | 0–450 V | 1 V | 線間電圧指令のRMS値。変調率 $m_a$ が変化 |
| f | f [Hz] | 1–200 Hz | 1 Hz | 出力周波数。基本波の周期が変化 |
| f_c | f_c [kHz] | 1–20 kHz | 0.1 kHz | キャリア周波数。スイッチング回数が変化 |
| t_d | t_d [us] | 0–10 us | 0.1 us | デッドタイム。零クロス歪みや基本波低下の要因 |
| V_on | V_on [V] | 0–5 V | 0.05 V | 導通経路の固定電圧降下。低電圧時の基本波低下を強調 |
| R | R [Ω] | 0.1–100 Ω | 0.1 Ω | 負荷抵抗。電流振幅が変化 |
| L | L [mH] | 0.1–100 mH | 0.1 mH | 負荷インダクタンス。電流のリプル・位相が変化 |

### 学習シナリオプリセット（IMPROVE-11）

スライダー下部に 6 つのプリセットボタンが並んでいます。クリックすると、対応する学習シナリオの初期パラメータが自動設定され、ヒントテキストが表示されます。

| ボタン | 初期パラメータの特徴 | 推奨操作 |
| --- | --- | --- |
| ①線形変調限界 | V_LL=141V, f_c=5kHz, L=10mH（デフォルト） | V_LLを180V→184V以上に上げて m_a クランプを観察 |
| ②キャリア周波数 | f_c=1kHz（低キャリア周波数） | f_cを1→10kHzに増やしてFFTの高調波移動を観察 |
| ③L平滑効果 | L=0.1mH（最小値付近）、FFT=電流 | Lを0.1→50mHに増やして電流リプルの減少を観察 |
| ④位相遅れ | L=50mH, R=5Ω（大きな時定数）、FFT=電流 | 情報パネルの cos(φ) と PF1(FFT) の値を確認 |
| ⑤過変調 | V_LL=220V（m_a>1） | Overmod View の ON/OFF でクランプ有無を比較 |
| ⑥SVPWM比較 | Reference=Min-Max, Sampling=Natural, Clamp=Continuous | Sinusoidal/THI と比較して線形上限と基本波利用率を観察 |

### エクスポート機能（IMPROVE-12）

プリセットボタンの下にエクスポートボタンがあります。

| ボタン | 機能 | 出力内容 |
| --- | --- | --- |
| JSON保存 | 現在のパラメータと主要指標をJSONファイルに保存 | params（8スライダー値・方式設定）+ metrics（m_a, m_f, V1, THD_V, I1, THD_I, PF1, φ, Z） |
| PNG保存 | 現在の波形ウィンドウをPNGファイルに保存 | 解像度150dpi、ファイル名はタイムスタンプ付き |
| ベースライン設定 | 現在の波形をベースラインとして記録 | ③線間電圧基本波（橙点線）と⑤相電流（紫点線）をオーバーレイ表示 |
| ベースライン解除 | ベースラインオーバーレイを削除 | — |

**ベースライン比較の使い方:**

1. 比較前の条件でシミュレーションを確認し、**ベースライン設定** をクリック
2. スライダーやモードを変更して条件を変える
3. ③線間電圧パネルと⑤電流パネルに、変更前のベースライン（点線）が重畳表示される
4. 情報パネルに `[ベースライン比較]` 行が追加され、V1・THD_V・I1 の差分が表示される
5. 比較が終わったら **ベースライン解除** をクリック

**保存ファイルの命名規則:** `pwm_export_YYYYMMDD_HHMMSS.json / .png`（カレントディレクトリに保存）

### 3.2 web UI

web UI の左カラムに 8 パラメータ入力、方式選択、学習シナリオガイド、比較/保存アクションがあります。

- 各パラメータはスライダーと数値入力を同期して変更できます
- 参照生成方式、サンプリング方式、クランプ方式、FFT 表示対象、FFT 窓関数はプルダウンで切り替えます
- 値変更後は 300 ms デバウンスで `/simulate` が再実行されます
- 「初期値に戻す」で全パラメータを web UI のデフォルト値へ戻せます
- シナリオボタンを押すと条件とガイド文が即座に切り替わります
- 「ベースライン設定」で現在条件を保存し、線間電圧基本波と相電流を点線オーバーレイできます
- 「JSON保存」で control 値 + 応答データ + ベースライン情報を保存できます
- 「PNG保存」で現在のダッシュボードを 1 枚の画像として保存できます

web UI では次を確認できます。

1. 変調信号とキャリアの関係
2. 線間電圧と相電圧の基本波オーバーレイ
3. 相電流と FFT の同時比較
4. m_a, m_f, THD_V, THD_I, V_rms, I_rms, PF1, I1_pk の指標
5. I_theory, I_measured, 誤差, cos(phi), PF1, phi の理論比較
6. ベースラインとの差分比較

## 4. 学習シナリオ

### シナリオ1: 変調率の効果を観察する

1. 他のパラメータはデフォルトのまま
2. Reference を **Sinusoidal Reference**、Clamp を **Continuous PWM** に設定し、Sampling を **Natural Sampling** または **Regular Sampling** に切り替える
3. **V_LL(rms)スライダー**を 0V → 200V まで徐々に増加
4. 観察ポイント:
   - ①のパネルで指令信号の振幅が大きくなる
   - ②のパルス幅が広くなる
   - ④の電流振幅が増加する
   - 左上の $m_a$ 値に注目（0→約0.77）
5. 約184V付近（$m_a \approx 1.0$）を超えるとクランプが有効なら**クランプ中**表示
   - これ以上V_LLを上げても出力は変化しない → **線形変調の限界**

### シナリオ2: キャリア周波数と高調波の関係

1. デフォルトパラメータで起動
2. **f_cスライダー**を 1kHz → 10kHz まで変化
3. 観察ポイント:
   - ⑥のFFTで、赤いキャリア高調波のピークが右に移動
   - f_cが高いほど**高調波が高周波側に移動→フィルタで除去しやすい**
   - ⑤の電流リプルが減少しやすくなる
   - ⑥のTHDは生のPWM線間電圧に対する指標なので、値の大小だけで良否を判断しない

### シナリオ3: RL負荷の平滑効果

1. R=10Ω, L=0.1mH（最小値付近）に設定
2. ④の電流波形にPWMリプルが大きく現れる
3. **Lスライダー**を 0.1mH → 50mH まで増加
4. 観察ポイント:
   - リプルが急激に減少し、滑らかな正弦波に近づく
   - **インダクタンスの平滑効果**を視覚的に確認
   - 同時に電流の位相遅れが大きくなる（電圧に対して遅れる）

### シナリオ4: 出力周波数と波形品質

1. V_LL(rms)=141V, f_c=5kHzで固定
2. **fスライダー**を 10Hz → 100Hz まで変化
3. 観察ポイント:
   - fが低い → 1周期あたりのスイッチング回数が多い → 波形品質が良い
   - fが高い → スイッチング回数が減る → 電流リプルが増加
   - $m_f = f_c / f$（周波数変調率）が重要な指標

### シナリオ5: 直流母線電圧の影響

1. V_LL(rms)=141V（固定）
2. **V_dcスライダー**を 200V → 600V まで変化
3. 観察ポイント:
   - V_dcが上がると③の線間電圧パルスが高くなるが、基本波成分は変わらない
   - $m_a$ が下がる → パルス幅が狭くなる
   - **同じ出力電圧を得るのにV_dcが高いほど変調率に余裕がある**

### シナリオ6: 非理想インバータの影響

1. V_LL(rms)=40V 程度の低電圧条件に設定
2. **t_d** を 0us → 4us、**V_on** を 0V → 1V に増加
3. 観察ポイント:
   - ③の線間電圧基本波振幅がわずかに低下する
   - ④の相電圧零クロス付近で、理想モデルより歪みが強くなる
   - ⑤の電流波形でも零クロス付近のゆがみが観察しやすくなる
   - 情報パネルの理論値比較は、非理想電圧から計算された基本波に追従する

### シナリオ7: 標準変調構成の比較

1. V_dc=300V, V_LL(rms)=170V, f=50Hz, f_c=5kHz, t_d=0us, V_on=0V に設定
2. 次の標準組み合わせを順に切り替える
   - Sinusoidal / Natural / Continuous
   - Sinusoidal / Regular / Continuous
   - 3rd Harmonic Injection / Natural / Continuous
   - Min-Max Zero-Sequence / Natural / Continuous
3. 観察ポイント:
   - Regular Sampling では①の参照波形が階段状になり、②のスイッチング時刻が変化する
   - 3rd Harmonic Injection と Min-Max Zero-Sequence では①の三相指令に零相成分が加わり、③の線間基本波利用率が増加する
   - ⑥の FFT で方式ごとの高調波分布の違いを比較できる
   - 同じ V_dc と V_LL 指令でも、参照生成方式とサンプリング方式の違いで線形変調の上限と高調波の見え方が変わる

### シナリオ8: 電圧スペクトルと電流スペクトルの比較

1. Reference を **Sinusoidal Reference**、Sampling を **Natural Sampling**、FFT Window を **Hann** に設定し、Clamp は任意に切り替える
2. FFT Signal を **Line Voltage v_uv** にして、⑥のキャリア高調波群を確認する
3. FFT Signal を **Phase Current i_u** に切り替える
4. 観察ポイント:
   - 電圧スペクトルではキャリア近傍の高調波が強く見える
   - 電流スペクトルでは RL 負荷の平滑効果で高周波成分が大きく減衰する
   - 情報パネルの **THD_V** と **THD_I** を比較すると、電流側が低くなりやすい
   - **PF1(FFT)** と理論の **cos(φ)** を比較すると、基本波位相差の理解に役立つ

## 5. 理論背景

### 変調率

$$m_a = \frac{2 V_{ph,peak}}{V_{dc}} = \frac{2 V_{LL,peak}}{\sqrt{3} \cdot V_{dc}} = \frac{2\sqrt{2} \cdot V_{LL,rms}}{\sqrt{3} \cdot V_{dc}}$$

- $0 \le m_a \le 1$: **線形変調領域**（出力基本波が指令に比例）
- Natural Sampling / Regular Sampling では $m_a > 1$ でクランプされる
- Third Harmonic Injection では線形上限が $m_a = 2 / \sqrt{3} \approx 1.155$ まで拡張される

### 線間電圧

$$v_{uv} = (S_u - S_v) \cdot V_{dc}$$

3レベル（$+V_{dc}$, $0$, $-V_{dc}$）のパルス電圧。

### 相電圧（負荷中性点基準）

$$v_{xN} = \frac{V_{dc}}{3}(2S_x - S_y - S_z) \quad (x, y, z = u, v, w)$$

### RL負荷の電流応答

$$v_{xN}(t) = R \cdot i_x(t) + L \cdot \frac{di_x(t)}{dt}$$

本シミュレータでは、各サンプル区間で相電圧を零次ホールド入力とみなし、
区分定数入力に対する RL 回路の厳密離散時間解

$$i_{x,n+1} = e^{-R dt / L} i_{x,n} + \frac{v_{x,n}}{R}\left(1 - e^{-R dt / L}\right)$$

を用いて電流を更新する。$R \to 0$ の極限では
$i_{x,n+1} = i_{x,n} + v_{x,n} dt / L$ と一致する。

定常状態での電流振幅の理論値:

$$I_{peak} = \frac{V_{ph,peak}}{\sqrt{R^2 + (2\pi f L)^2}}$$

### THD（全高調波歪み率）

$$\text{THD} = \frac{\sqrt{\sum_{n \neq 1} V_n^2}}{V_1} \times 100\%$$

## 6. 注意事項

- **助走区間**: シミュレーションは自動的に十分な助走区間を計算し、表示は定常状態の最後2周期のみです。パラメータによっては計算に数秒かかることがあります。
- **線形変調の上限**: Sinusoidal Reference は $m_a \le 1$、3rd Harmonic Injection と Min-Max Zero-Sequence は $m_a \le 2 / \sqrt{3}$ を線形上限とします。Overmod View を有効にするとクランプを無効化して観察できます。
- **参照生成方式の違い**: Sinusoidal Reference は純正弦基準、3rd Harmonic Injection は零相三次高調波注入、Min-Max Zero-Sequence は空間ベクトル等価の零相注入方式です。
- **サンプリング方式の違い**: Natural Sampling は連続比較、Regular Sampling はキャリア周期ごとの保持比較です。
- **クランプ方式の違い**: Continuous PWM は連続PWM、DPWM1/DPWM2/DPWM3 は不連続PWM（1相クランプ）のクランプ位置違いとして動作します。
- **理論比較の意味**: 情報パネルの電流比較は、生波形のピーク値ではなく FFT で抽出した基本波振幅を用いています。
- **FFT 表示の意味**: v_uv 表示ではスイッチング起因の高調波分布、i_u 表示では RL 負荷で平滑化された結果を比較できます。
- **窓関数の意味**: Hann 窓は leakage を抑えて非整数周期長でも安定した振幅推定をしやすくします。Rectangular は整数周期条件で直感的なスペクトルを見やすい設定です。
- **RL ソルバの前提**: 電流更新は区分定数の相電圧に対する厳密離散時間解を用いています。PWM ステップ入力に対して RK4 より前提が明確です。
- **デッドタイム/電圧降下モデル**: 簡易非理想モデルとして、デッドタイム中は相電流の向きで導通ダイオードを切り替え、導通経路には固定電圧降下 $V_{on}$ を与えています。
- **非理想モデルの簡略化**: 導通電圧降下は固定値であり、素子のオン抵抗、回復特性、DCリンクリプル、温度依存性は扱っていません。

## 7. トラブルシューティング

| 症状 | 対処法 |
| --- | --- |
| ウィンドウが表示されない | Matplotlibのバックエンド確認: `matplotlib.get_backend()` |
| web UI が開かない | `python -m uvicorn webapi.app:app --reload` が起動しているか、`http://127.0.0.1:8000/` を開いているか確認 |
| `/simulate` が失敗する | 入力値が API 範囲外だと 422 になる。V_dc, V_LL, f_c, L などの範囲を確認 |
| スライダー操作が重い | f_cを下げる（計算点数が減少） |
| 電流波形が不安定 | Lが極端に小さい場合（<0.1mH）、リプルが大きくなるのは正常動作 |
| V_LLを上げても出力が変わらない | 選択中方式の線形上限でクランプ中。左上の $m_a$ 表示で上限を確認 |
